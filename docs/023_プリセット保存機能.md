# 023: プリセット保存機能

## Phase
Phase 3 - 差別化強化

## 概要
よく使うメール作成設定（送信相手・目的・トーン等の組み合わせ）をプリセットとして保存・呼び出しできる機能。

## TODO

- [ ] プリセット保存機能の実装
  - [ ] こだわり作成完了時に「この設定をプリセットとして保存」ボタン
  - [ ] プリセット名の入力ダイアログ
  - [ ] 保存するデータ: 送信相手設定 / 目的 / トーン設定
- [ ] プリセット管理画面の実装
  - [ ] プリセット一覧表示
  - [ ] プリセットの編集・削除
  - [ ] プリセット名の変更
- [ ] プリセットからの作成フロー
  - [ ] ホーム画面 or こだわり作成画面からプリセット選択
  - [ ] 設定の自動入力 → 追加情報の入力 → 生成
- [ ] バックエンドCRUD
  - [ ] POST /presets — 作成
  - [ ] GET /presets — 一覧取得
  - [ ] PUT /presets/:id — 更新
  - [ ] DELETE /presets/:id — 削除

## 依存
- 003_データベース設計（presetsテーブル）
- 008_こだわり作成画面
- 014_設定画面

## 完了条件
- メール作成設定をプリセットとして保存できる
- プリセットから設定を呼び出してメールを作成できる
- プリセットの編集・削除ができる
